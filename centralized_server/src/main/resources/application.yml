spring:
  application:
    name: "centralized_server"
  datasource:
    url: jdbc:mysql://localhost:3307/thesis
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 8080